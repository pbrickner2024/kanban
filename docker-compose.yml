services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PM_DB_PATH: /data/pm.db
    volumes:
      - ./data:/data
    restart: unless-stopped
